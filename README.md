# Zenora — build handoff pack

1. Create an empty Git repo (e.g. `zenora`) and copy this whole folder into it.
2. Open the repo in Claude Code and paste the prompt from `KICKOFF_PROMPT.md`.
3. Clickable design: https://claude.ai/artifact/CWN39QTMKSK1eqAZ97dSSq

Contents: `CLAUDE.md` (auto-read project memory for Claude Code), `docs/` (PRD, architecture, integrations and costs, plans and limits, roadmap), `design/` (screen index + 72 reference screens + flow map).
